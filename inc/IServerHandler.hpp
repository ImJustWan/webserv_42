// #ifndef __ISERVERHANDLER__
// #define __ISERVERHANDLER__

class	IServerHandler
{
	private:


	public:
		// virtual			~IServerHandler() {};
		// virtual void	createServers( std::map<std::string, int> config_file ) = 0;
		// virtual void	closeServers( Server& servers ) = 0;

};

// #endif
